apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jerney-root
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Sudarshan1-Git/Jerney-End-to-End-Project.git
    targetRevision: main
    path: argocd/applications

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true